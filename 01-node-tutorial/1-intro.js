const amount = 12;

if (amount < 10) {
  console.log("small number");
} else {
  console.log("large number");
}

console.log(`hey it's my first node app!!!`);

// Run the file in the terminal using the following command:
// node 1-intro.js
// (ensure you are in the correct directory in the terminal or provide the full path to the file)
// example: node node-tutorial/1-intro.js
